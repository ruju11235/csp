// Implement https://github.com/ruju11235/csp/issues/11

// Evaluates a given polynomial when applied to
// a list of coefficients cs and real number a.
function evaluate(cs, a)
{
    if (cs.length === 1)
    {
        return a * cs[0];
    }
    else
    {}
}

var cs = [2]
console.log(evaluate(3,cs));